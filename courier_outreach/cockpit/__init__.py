"""Cockpit + booking.

- sheets.py   — push the leads table to your Google Sheet (the human cockpit).
- calendar.py — on a confirmed booking, create the Google Calendar event and
                email the lead a confirmation.
- clients.py  — build authenticated Google API services from the service account.
"""
