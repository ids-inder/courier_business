# --- Core ---
python-dotenv>=1.0.1          # load .env
tenacity>=8.5.0               # retry/backoff for network calls
tzdata>=2024.1               # timezone db (Asia/Kolkata) on slim VPS images

# --- The brain ---
anthropic>=0.40.0             # Claude API: drafting, reply classification

# --- Sourcing (Google Places discovery + company-website email extraction) ---
httpx>=0.27.0                 # HTTP client
beautifulsoup4>=4.12.3        # parse company websites for published emails
lxml>=5.2.0                   # fast parser backend for bs4

# --- Google Sheet (cockpit) + Calendar (booking) ---
google-api-python-client>=2.137.0
google-auth>=2.32.0

# --- The mini "Approvals & Chat" web app ---
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
jinja2>=3.1.4                 # server-rendered console templates
python-multipart>=0.0.9      # form handling for the console

# --- Dev / quality ---
pytest>=8.2.0
ruff>=0.5.0
